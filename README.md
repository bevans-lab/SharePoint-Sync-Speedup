# SharePoint Sync Speedup

A C++ utility to improve SharePoint file synchronization performance.

## Build

```powershell
mkdir build
cd build
cmake ..
cmake --build .
```

## Run

```powershell
.\build\SharePointSyncSpeedup.exe
```
